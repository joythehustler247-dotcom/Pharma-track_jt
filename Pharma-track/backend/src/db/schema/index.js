/**
 * Define your Drizzle schemas here using drizzle-orm/pg-core.
 * Example:
 * const { pgTable, serial, text } = require('drizzle-orm/pg-core');
 * const users = pgTable('users', { id: serial('id').primaryKey(), email: text('email').notNull() });
 * module.exports = { users };
 */

module.exports = {};
